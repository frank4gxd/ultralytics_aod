# ultralytics basics
data: dataset_min.yaml   # <-- change me
model: yolo12n_aod.yaml              # base weights
imgsz: 640
batch: 12
epochs: 200
device: 0
amp: true

wandb:
  enabled: true
  project: "yolo_dehazing"
  entity: "self-driving-team"